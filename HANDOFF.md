# GameLab HANDOFF

Godotゲーム開発の規約とノウハウの置き場。実験の結果を貯め、本番開発で読み込む。

## 使い方

新しいチャットの冒頭に、この `HANDOFF.md` と該当する `RULE_*.md` を貼る。
Claude Project のナレッジに `RULE_*.md` を入れておけば、貼る手間も省ける。
GitHubが正本、Projectが配布先。

## ファイル

| ファイル | 内容 | 状態 |
|---|---|---|
| `RULE_ART.md` | 画面解像度、スプライトのサイズと色数、素材の作り方、採用済みの表現技法 | 確定 |
| `RULE_CODE.md` | GDScriptの書き方、型推論の落とし穴、CI、配布の型 | 確定 |
| `RULE_FEEL.md` | 速度・重力・操作感のパラメータ | 未着手 |
| `RULE_UI.md` | タッチ操作、ボタン配置 | 未着手 |
| `LOG/` | 実験1本ごとの記録 | 001完了 |

## 役割分担

| 工程 | 担当 |
|---|---|
| コードを書く | Claude |
| CIを通す、エラーを潰す | Claude |
| 動きを付ける | Claude |
| 写真・素材の調達 | 関口 |
| APKを触って判定する | 関口 |
| push する | 関口 |

**エラーの一次発見者はClaude。** 関口さんに渡るのはビルドが通ったAPKだけ。

## 開発の流れ

1. **フェーズ0** — CIを通す。中身より先にビルド疎通を確認する（省略禁止）
2. **フェーズ1** — テストアプリで検証。1本に必要な要素をまとめて入れる
3. **フェーズ2** — 結果を `RULE_*.md` に確定させる
4. **フェーズ3** — 貯まった規約で本番を組む
5. **フェーズ4** — 本番で気づいたことを規約に戻す

## 次にやること

- [ ] 本番で作るゲームを決める
- [ ] 日本語表示の方針を決める
- [ ] `RULE_FEEL.md` を作る（操作感パラメータの検証が必要）
